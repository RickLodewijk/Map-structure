# Create folder structure

This is a project to create folders in structure with a simple interface.

It is build in python. You run the exe file and run that. You fill in the name of the project select where the folders should be created

Folders build are:
```   
    "01_Footage",
    "02_Project",
    "03_Audio",
    "04_Export"
```
